export interface WorkItem {
  title: string;
  description: string;
  url: string;
  image: string;
  category: string;
  language: string[];
}

export interface TechItem {
  name: string;
}

export interface ArtItem {
  title: string;
  image: string;
  category: string;
}
